openapi: '3.0.3'
info:
  title: Career Copilot API
  description: AI-Powered Career Development Platform API
  version: '1.0.0'
  contact:
    name: Career Copilot Team

servers:
  - url: http://localhost:4000/api/v1
    description: Local development
  - url: http://localhost:8000/api/v1
    description: AI Engine (FastAPI)

paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is healthy

  # TODO: Add full endpoint specs for each module
  # /auth
  # /users
  # /companies
  # /jobs
  # /applications
  # /resumes
  # /cover-letters
  # /recruiters
  # /dashboard
  # /analytics
